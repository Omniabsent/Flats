<!DOCTYPE html>
<html>
  <head>
    <title>Flats</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <nav>
      <% if place_owner_signed_in? %>
        <p><%= current_place_owner.email %></p>
        <p><%= link_to 'Logout', destroy_place_owner_session_path, method: :delete %></p>
        <p><%= link_to '> Cadastrar imóvel', new_place_path %></p>
      <% else %>
        <p><%= link_to 'Entrar', new_place_owner_session_path %></p>
      <% end %>
    </nav>
    <p><%= flash[:notice] %></p>
    <p><%= flash[:alert] %></p>
    <%= yield %>
  </body>
</html>
