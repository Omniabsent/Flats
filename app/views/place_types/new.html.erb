<% if @place_type.errors.any? then %>
  <% @place_type.errors.full_messages.each do |error| %>
    <%= error %>
  <% end %>
<% end %>

<%= form_with(model: PlaceType.new) do |pt| %>
    <%= pt.label :name, 'Novo tipo: ' %>
    <%= pt.text_field :name %>
    <%= pt.submit 'Enviar' %>
<%end%>
